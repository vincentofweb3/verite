{
  "name": "verite-rules-tests",
  "private": true,
  "scripts": {
    "test:rules": "firebase emulators:exec --project verite-rules-test --only firestore \"node tests/firestore-rules.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.3.1",
    "firebase-tools": "^15.28.1"
  }
}
